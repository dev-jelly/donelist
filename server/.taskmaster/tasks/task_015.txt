# Task ID: 15
# Title: Complete Kubernetes Deployment Configuration
# Status: pending
# Dependencies: 10, 11
# Priority: medium
# Description: Finalize K8s/K3s deployment with auto-scaling, monitoring, and zero-downtime deployments
# Details:
Complete K8s manifests in deploy/k8s/: configure HorizontalPodAutoscaler based on CPU/memory, implement rolling update strategy with proper health checks, add NetworkPolicy for security, configure PersistentVolumeClaims for data, implement Secrets management with sealed-secrets, add Prometheus ServiceMonitor for scraping, configure Ingress with TLS termination, implement init containers for migrations.

# Test Strategy:
Test deployment rollout, verify auto-scaling triggers, test rolling updates, verify persistent storage, test monitoring integration

# Subtasks:
## 1. Configure HorizontalPodAutoscaler with CPU and memory metrics [pending]
### Dependencies: None
### Description: Set up HorizontalPodAutoscaler (HPA) for the main application deployment with CPU and memory-based scaling rules, target utilization thresholds, and min/max replica counts
### Details:
Create HPA manifest in deploy/k8s/hpa.yaml defining: minReplicas: 2, maxReplicas: 10, CPU target utilization: 70%, memory target utilization: 80%. Configure metrics-server if not present. Set appropriate resource requests/limits in deployment spec for HPA to function correctly. Test scaling behavior under load.

## 2. Implement rolling update strategy with health checks [pending]
### Dependencies: None
### Description: Configure deployment strategy for zero-downtime updates with proper readiness and liveness probes, graceful shutdown handling, and rollback capabilities
### Details:
Update deployment manifest with: strategy type RollingUpdate, maxSurge: 1, maxUnavailable: 0. Add readinessProbe with httpGet to /health endpoint (initialDelaySeconds: 10, periodSeconds: 5). Add livenessProbe (initialDelaySeconds: 30, periodSeconds: 10). Implement graceful shutdown with preStop hook and SIGTERM handling. Set progressDeadlineSeconds and revisionHistoryLimit.

## 3. Add NetworkPolicy for pod-to-pod security [pending]
### Dependencies: None
### Description: Create NetworkPolicy resources to restrict network traffic between pods, allowing only necessary communication paths and blocking unauthorized access
### Details:
Create NetworkPolicy in deploy/k8s/network-policy.yaml: Allow ingress from ingress-nginx namespace to app pods on port 8080, allow app pods to connect to database pods on port 5432, allow app pods to connect to Redis on port 6379, deny all other inter-pod communication by default. Add labels to pods for policy selection. Document the network flow in comments.

## 4. Configure PersistentVolumeClaims for data storage [pending]
### Dependencies: None
### Description: Set up persistent storage for application data, database, and file uploads using PersistentVolumeClaims with appropriate storage classes and access modes
### Details:
Create PVC manifests in deploy/k8s/pvc.yaml: Database PVC with 10Gi capacity using SSD storage class, file uploads PVC with 20Gi using standard storage, Redis data PVC with 5Gi for persistence. Set accessMode: ReadWriteOnce for database/Redis, ReadWriteMany for shared uploads. Update deployments to mount these volumes. Add backup CronJob for persistent data.

## 5. Implement Secrets management with sealed-secrets [pending]
### Dependencies: 15.4
### Description: Deploy sealed-secrets controller and convert all sensitive configuration to encrypted SealedSecret resources for secure GitOps workflow
### Details:
Install sealed-secrets controller in kube-system namespace using official helm chart or manifest. Generate encryption keys and backup securely. Convert existing secrets (database credentials, API keys, JWT secrets) to SealedSecrets using kubeseal. Create deploy/k8s/sealed-secrets/ directory for encrypted secrets. Update deployment manifests to reference the secrets. Document the sealing/unsealing process in deploy/k8s/README.md.

## 6. Add Prometheus ServiceMonitor and configure Ingress with TLS [pending]
### Dependencies: 15.5
### Description: Create ServiceMonitor for Prometheus scraping and configure Ingress with TLS termination, proper routing rules, and init containers for database migrations
### Details:
Create ServiceMonitor in deploy/k8s/monitoring/servicemonitor.yaml targeting app's /metrics endpoint with 30s scrape interval. Configure Ingress in deploy/k8s/ingress.yaml with: cert-manager annotations for Let's Encrypt TLS, proper host routing rules, rate limiting annotations, WebSocket support for /ws path. Add init container in deployment for database migrations using migrate tool, ensuring migrations run before main container starts.

